<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <!--mvc-->
    <context:component-scan base-package="controler"/>
    <mvc:annotation-driven/>
    <!--视图解析器-->
    <bean id="internalResourceViewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="suffix" value=".jsp"/>
        <property name="prefix" value="/pages/"/>
    </bean>
    <!--释放静态资源-->
    <mvc:default-servlet-handler/>
    <!--拦截器配置-->
    <mvc:interceptors>
        <!--    1.拦截所有请求-->
<!--        <bean class="interceptor.CustomInterceptor"/>-->

        <!--    2.<mvc:interceptor> 标签用于拦截指定路径下的请求-->
        <mvc:interceptor>
            <!--配置拦截器的作用路径 /** 拦截所有-->
            <mvc:mapping path="/**"/>
            <!--配置不需要拦截的路径 路径指的是
            http://localhost:9002/index/toLogin
            /index/** 意思指的是不拦截请求路径/login/下的所有文件
            -->
            <mvc:exclude-mapping path="/index/toLogin"/>
            <mvc:exclude-mapping path="/index/login"/>
            <bean class="interceptor.CustomInterceptor"/>
        </mvc:interceptor>
        <!--拦截器2-->
        <mvc:interceptor>
            <mvc:mapping path="/**"/>
            <mvc:exclude-mapping path="/index/toLogin"/>
            <mvc:exclude-mapping path="/index/login"/>
            <bean class="interceptor.CustomInterceptor1"/>
        </mvc:interceptor>
    </mvc:interceptors>
</beans>